# ShellQuest 课程与关卡设计（初版）

> 本文定义 21 个课程单元的主题、目标与场景任务方向。每个单元可在任意日期完成；“第 N 天”仅表示推荐顺序。

## 内容组织规则

- 每个单元建议包含：1 个短知识卡、2～3 个场景任务、1 个快速巩固题。
- 场景任务优先采用“问题描述 → 给定上下文 → 用户操作 → 反馈解析”的结构。
- 快速巩固题只负责检查基础命令与参数记忆；单元完成评价以场景任务为主。
- 前 7 单元建立基础操作能力，中间 9 单元覆盖常见开发/运维现场，最后 5 单元以 Docker 与综合排障收束。

## 主题地图

| 区域 | 单元 | 技能节点 |
| --- | --- | --- |
| 文件工坊 | 1～4 | 文件、目录、文本、权限 |
| 系统哨站 | 5～9 | 用户、进程、服务、磁盘、日志 |
| 网络前线 | 10～12 | 网络连通、端口、远程传输 |
| Shell 作战室 | 13～16 | 管道、重定向、变量、脚本 |
| 容器基地 | 17～19 | Docker 镜像、容器、网络与数据 |
| 故障指挥中心 | 20～21 | 综合排障与命令决策 |

## 21 个课程单元

| 单元 | 主题 | 场景任务示例 | 重点命令/知识 |
| --- | --- | --- | --- |
| 01 | 初入服务器 | 刚登录测试机，需要确认当前身份、所在目录与系统基本信息。 | `whoami`、`pwd`、`uname`、`hostname` |
| 02 | 项目文件定位 | 发布包解压后找不到配置文件，需要按名称和类型定位。 | `ls`、`find`、`locate`、通配符 |
| 03 | 文件整理与备份 | 将散落的日志归档到指定目录，且不能误覆盖已有备份。 | `cp`、`mv`、`mkdir`、`rm`、选项安全性 |
| 04 | 文本内容追踪 | 从多份配置中找出错误的环境变量配置，并查看附近内容。 | `cat`、`less`、`head`、`tail`、`grep` |
| 05 | 权限事故 | 部署脚本无法执行，需判断是文件权限、属主还是目录权限导致。 | `chmod`、`chown`、`chgrp`、权限位 |
| 06 | 用户与访问 | 新同事需要受限访问日志目录，避免授予过高权限。 | `id`、`useradd`、`groups`、`sudo` |
| 07 | 失控的进程 | 本地服务卡死且占用 CPU，需要定位进程并安全结束。 | `ps`、`top`、`pgrep`、`kill`、信号 |
| 08 | 服务没有启动 | API 服务重启后不可用，需检查服务状态和启动失败原因。 | `systemctl`、`journalctl` |
| 09 | 磁盘告警 | 服务器提示磁盘空间不足，判断是哪个分区、目录或日志占满。 | `df`、`du`、`ls -lh` |
| 10 | 网络不通 | 应用无法访问内部接口，按 DNS、网络连通与 HTTP 响应逐步检查。 | `ping`、`curl`、`nslookup`/`dig` |
| 11 | 端口冲突 | 新服务无法启动，日志显示端口已被占用，定位并处理占用者。 | `ss`、`lsof`、`netstat`（认知） |
| 12 | 远程发布 | 将构建产物安全传到测试机并验证文件是否完整。 | `ssh`、`scp`、`rsync` |
| 13 | 日志筛查管道 | 在大量访问日志中筛选异常状态码，并统计最常见的来源。 | 管道 `|`、`grep`、`sort`、`uniq`、`wc` |
| 14 | 输出与错误 | 执行构建任务时同时保存正常输出与错误日志，方便复盘。 | `>`、`>>`、`2>`、`2>&1`、`tee` |
| 15 | 批量维护 | 给一批文件统一改名或批量检查配置内容，避免逐个手工处理。 | `xargs`、`find -exec`、Shell 展开 |
| 16 | 值班小脚本 | 编写一个轻量脚本检查服务是否存活，并给出可读结果。 | 变量、条件判断、退出码、`echo` |
| 17 | 镜像构建失败 | 根据 Dockerfile 和构建日志定位失败层，修复镜像构建命令。 | `docker build`、`docker images`、Dockerfile 基础 |
| 18 | 容器未响应 | 容器已启动但接口不可用，检查状态、日志、进程和端口映射。 | `docker ps`、`docker logs`、`docker exec`、`docker port` |
| 19 | 数据不会丢 | 数据库容器重建后数据丢失，需要设计并验证卷挂载。 | `docker volume`、挂载、`docker compose` |
| 20 | 深夜发布故障 | 发布后页面 502：从服务状态、端口到反向代理日志进行链式排障。 | 前述命令的组合与排障顺序 |
| 21 | 终局演练 | 一台开发环境机器出现磁盘满、容器重启和接口超时，选择最小风险的处理方案。 | 诊断优先、证据收集、命令组合 |

## 场景任务模板

```text
任务名称：端口冲突：谁占用了 8080？

背景：部署新版本 API 时，启动日志提示“Address already in use”。
上下文：提供服务启动日志，以及 `ss -ltnp` 的模拟输出。
目标：定位占用 8080 端口的进程，并选择合适的下一步处理方式。
限制：不能直接重启整台服务器；不能误杀无关进程。
用户操作：在模拟终端输入查询命令，并选择结束/保留进程的策略。
反馈：展示结果、参考命令、风险提示和延伸学习链接。
```

## 题型配比建议

| 类型 | 建议占比 | 用途 |
| --- | --- | --- |
| 场景化模拟终端与结果判断 | 60% | 训练诊断思路与命令组合。 |
| 场景内命令填空 | 25% | 巩固关键命令、参数与管道。 |
| 选择题与速答 | 15% | 快速复习概念、风险和输出含义。 |
