node_modules
dist
.venv
__pycache__
*.py[cod]
*.db
# SQLite 的旁挂文件与手工备份。`*.db` 只覆盖主库文件本身，
# 重建库时顺手改名的 shellquest.db.bak-* 会漏网（曾差点提交一个 8MB 的库备份）。
*.db-journal
*.db-wal
*.db-shm
*.db.bak*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 编辑器与 AI 助手工作数据，不属于项目代码
.vscode/
.workbuddy-ai/

# pnpm 在项目目录下产生的临时探测文件
_tmp_*

# 命令手册导入脚本下载的上游包缓存（内容已导出为 backend/data/commands_seed.json.gz）
backend/.cache/
