# ShellQuest 需求文档

> 面向全栈开发者与架构师的 21 天 Linux 实战训练。
>
> 本文档记录第一期产品范围；未定内容会明确标注，避免过早扩大范围。

## 1. 项目定位

ShellQuest 是一个兼具学习、练习与速查能力的 Linux 命令训练 Web 应用。它以简洁的程序员风格界面，将命令学习设计为可打卡的任务闯关，并通过技能树成长给予持续反馈。

### 核心目标

- 帮助用户在 21 天内建立常用 Linux 命令的实战能力。
- 让用户可按固定课程或自由主题两种路径学习。
- 让用户通过命令速查和自然语言搜索，快速找到正确命令与用法。
- 用轻量游戏化机制提高完成率，但不牺牲专业性与学习效率。

## 2. 目标用户

- 全栈开发者、后端开发者、运维初学者及架构师。
- 有命令行基础或需要系统补齐 Linux 实战能力的技术人员。
- 希望利用碎片时间完成短练习、并能即时查询命令的用户。

## 3. 训练路径

用户首次开始训练时，可选择以下任一模式；之后允许切换。

| 模式 | 说明 |
| --- | --- |
| 21 天训练营 | 按 21 个课程单元循序学习、支持每日打卡，适合系统入门。 |
| 自由闯关 | 从主题地图中自由选择任务，适合按当前工作需求练习。 |

首期主题包含：文件与目录、文本处理、用户与权限、进程与服务、网络、磁盘与日志、Shell 基础、Docker 与基础排障。

### 训练进度规则

- “21 天”是课程内容的组织方式和建议节奏，不是完成任务的时间限制。
- 用户可以在同一天完成多个课程单元，也可以一次完成全部训练任务。
- 课程解锁只受前置知识或关卡规则影响，不按现实日期强制限制。
- 打卡用于记录学习行为与连续性，不应阻碍已解锁课程的学习进度。

### 实现状态

**已实现。** 两种路径由 `users.training_mode` 承载（取值 `camp` / `free`，默认 `camp`），
切换接口 `POST /api/v1/user/mode`，前端入口在课程地图页（`TrainingModeSwitch.vue`）。

| 模式 | 单元状态表现 | 越级作答 |
| --- | --- | --- |
| `camp` 21 天训练营 | 严格串行：`done` / `current` / `locked` | 403 拦截 |
| `free` 自由闯关 | 未完成单元一律 `available`，不设锁 | 允许 |

两种模式**共用同一份完成记录与经验值**，因此「之后允许切换」是零成本的：切换只改变
`compute_unit_statuses()` 是否上锁，不丢进度、不需要数据迁移。已完成单元的 `done` 状态在两种模式下一致。

> 前端展示：`available` 渲染为可点击的「可挑战」卡片（区别于 `current` 的「进行中」）；
> `free` 模式下不存在 `locked` 卡片。浏览器已往返验证 `camp ⇄ free`。

## 4. 第一期功能范围（MVP）

### 4.1 课程与任务

- 提供完整的 21 天课程；每一天包含学习要点、命令说明和若干练习任务。
- 提供按主题组织的关卡列表与进度展示。
- 每个任务以真实开发或运维场景描述目标、前置条件与预期结果。
- 任务难度分级，并显示推荐学习顺序。

首期上线即提供完整的 21 个课程单元；每个单元包含 2～3 道场景任务与 1 道快速巩固题。

### 4.1.1 场景化出题原则

- 练习以场景题为主体：用户需要先理解问题，再选择或组合命令解决问题。
- 场景应贴近日常全栈开发、部署、排障与服务器维护，例如发布失败、磁盘告警、端口冲突、日志定位、权限异常。
- 每道场景题应明确提供必要上下文（目录结构、日志片段、命令输出或限制条件），避免依赖猜测。
- 命令填空、参数辨析和选择题用于巩固知识点，可作为场景任务中的子步骤，不应成为全部题目。
- 解析除给出标准命令外，还应说明选择该命令的原因、常见误区与更安全的替代方案。

### 4.1.2 题型与数据模型

本节定义课程内容的数据结构，是 4.2 练习交互与 4.4 命令联动的共同基础。

#### 题型

首期支持 4 种题型，与 4.2 的交互形式一一对应：

| 题型标识 | 名称 | 交互形式 | 判题依据 |
| --- | --- | --- | --- |
| `terminal` | 模拟终端 | 终端风格界面输入命令，返回预设输出 | 命令文本 |
| `fill` | 命令填空 | 补全命令、参数或管道组合 | 填空文本 |
| `choice` | 选择题 | 选择唯一正确项 | 选项 id |
| `judge` | 结果判断 | 给出命令与输出，判断对错或选择下一步 | 选项 id |

#### 判题规则

判题必须由**结构化规则**驱动，**不允许在业务代码里写死字符串匹配**（现有 `input.includes('find')` 的做法必须废弃）。

| `judge_type` | 含义 | 载荷 |
| --- | --- | --- |
| `contains_all` | 归一化后必须包含全部关键词 | `terms: string[]` |
| `contains_any` | 归一化后包含任一关键词 | `terms: string[]` |
| `regex` | 正则匹配 | `pattern: string` |
| `equals` | 归一化后完全相等 | `value: string` |
| `option` | 比对选项 id | `correct_keys: string[]` |

**输入归一化**（`terminal` / `fill` 判题前统一执行）：

- 去除行首提示符（`$`、`#`、`[root@host ~]#` 等）
- 折叠连续空白为单个空格
- 统一引号：`“”‘’` → `"` `'`
- 去除首尾空白

#### 数据模型

课程内容分三层：**课程单元 → 题目 → 选项**。

```
CourseUnit（21 条）
  order            1..21，唯一，决定推荐顺序
  zone             所属主题区域（见 COURSE_DESIGN.md 的 6 个区域）
  title            单元标题
  goal             学习目标（一句话）
  knowledge        知识卡正文（Markdown）

Quest（每单元 2~3 道场景任务 + 1 道快速巩固题）
  unit_id          → CourseUnit
  order            全局唯一，决定解锁顺序
  kind             terminal | fill | choice | judge
  title            任务名称
  scenario         场景描述（问题背景）
  context          给定上下文（目录树 / 日志片段 / 命令输出），Markdown
  prompt           题干或输入提示
  answer_display   参考答案（仅用于展示，不作为判题依据）
  explanation      解析：为什么这样选
  pitfalls         常见误区
  safer_alt        更安全的替代方案
  judge_type       见上表
  judge_payload    判题载荷（JSON）
  difficulty       1~3
  xp_reward        完成奖励

QuestOption（仅 choice / judge 使用）
  quest_id         → Quest
  key              A / B / C / D
  text             选项文本
  is_correct       是否正确

QuestCommand（题目 ↔ 命令，供 4.4 双向联动）
  quest_id         → Quest
  command_name     命令名，对应命令表中的 name
```

#### 技能节点归属

技能节点**绑定课程单元**，不绑定单道题目（对齐 `COURSE_DESIGN.md` 的主题地图：区域 → 单元 → 技能节点）。

- 完成一个单元的**全部题目** → 点亮该单元对应的技能节点。
- 部分完成只累计进度，不点亮节点。

#### 与现有实现的关系

现有 `Quest` 表把「课程单元」与「题目」混为一谈：21 个单元各只有一条记录，且字段只有 5 个纯文本，导致任务页的场景描述与终端提示**只能写死在模板里**，21 关点进去内容相同。

调整为上述三层模型后：

- 每个单元拥有自己的知识卡与场景内容。
- 每道题拥有自己的题干、上下文、判题规则与解析。
- 题目与命令建立显式关联，支撑 4.4 的双向跳转。

#### 实现状态

| 项 | 状态 |
| --- | --- |
| 三层模型（`CourseUnit` / `Quest` / `QuestOption` / `QuestCommand`） | ✅ 已实现 |
| 4 种题型 | ✅ 已实现，63 道题覆盖全部 4 种 |
| 5 种判题器 + 输入归一化 | ✅ 已实现（`main.py` 的 `judge_answer` / `normalize_answer`） |
| 21 个单元的课程内容 | ✅ 已实现，位于 `backend/app/curriculum/` |
| 内容自检 + 幂等落库 | ✅ 已实现（`validate_curriculum` / `seed_curriculum`） |
| 提交判题接口 | ✅ `POST /api/v1/quests/{id}/submit` |
| 前端四种题型的交互界面 | ✅ `QuestionPane.vue` 按 `kind` 分派（见 4.2） |
| 前端课程地图（按区域分组的 21 个单元） | ✅ `UnitsView.vue` |
| 前端单元作答页（题目列表 + 知识卡片） | ✅ `UnitView.vue` |
| 命令查询与双向跳转（4.4） | ✅ 已实现（`CommandsView.vue` / `CommandDetailView.vue`，见 4.4.12） |

**判题边界（已知取舍）**

`contains_all` / `contains_any` 是**关键词包含**，不校验顺序、不校验参数个数，因此
`terms: ["find", ".env"]` 会同时接受 `find /x -name .env` 与 `echo find .env`。
这是为了容忍等价写法（参数顺序、绝对/相对路径、引号风格）而付出的代价；
需要严格匹配时使用 `regex` 或 `equals`。

答案字段（`answer_display` / `judge_payload` / 选项的 `is_correct`）**只在提交接口返回**，
课程列表与题目详情接口不下发，避免前端直接读到答案。

### 4.2 练习交互

首期不提供真实可执行的 Linux 容器终端，采用低风险、即时反馈的模拟练习：

- 命令填空：补全命令、参数或管道组合。
- 选择题：识别命令用途、参数含义和输出结果。
- 模拟终端：在终端风格界面输入命令，根据预设场景返回模拟输出。
- 结果判断：根据命令与输出判断操作是否正确，或选择应执行的下一步。

答题后应即时显示正确与否、参考命令、简短解释及相关命令链接。

四种交互形式与 4.1.2 的题型标识**一一对应**：模拟终端 = `terminal`，命令填空 = `fill`，
选择题 = `choice`，结果判断 = `judge`。前后端均已落地：后端判题与作答接口 + 前端
`QuestionPane.vue` 按题型分派作答控件。

**`fill` 题型的题干与判题口径存在偏差**：题干写「补全命令：`uname -____`」，但判题器要求答案包含
`uname` 与 `-r`，即必须提交**完整命令**。前端已在输入框上方标注「填写完整命令（含命令名）」兜住，
但更彻底的做法是统一 13 道 `fill` 题的题干措辞（见 `AGENTS.md` §7）。

**`choice` / `judge` 目前全部为单选**（27 道题的 `correct_keys` 长度均为 1），故前端用单选交互；
若以后出现多选题，作答控件需要改成多选。

- 作答提交：`POST /api/v1/quests/{quest_id}/submit`
  - `terminal` / `fill` 提交 `{"answer": "<命令文本>"}`
  - `choice` / `judge` 提交 `{"option_keys": ["A"]}`
- 响应统一返回：`correct`、`expected_display`（参考答案）、`correct_keys`、
  `explanation`、`pitfalls`、`safer_alt`、`xp_awarded`、`unit_completed`、`user`。
- **答错也返回完整解析**，不区分对待，便于即时学习。
- 重复提交同一题返回 `already_completed=true, xp_awarded=0`，但**依然返回解析**。

### 4.3 游戏化机制

- 任务闯关：完成前置任务后解锁后续关卡或主题区域。
- 技能树成长：完成任务获得经验值，点亮对应技能节点（如文件、网络、Docker、排障）。
- 每日打卡：完成当天课程或至少一个有效任务后记录连续学习天数。
- 成就与称号：为连续打卡、主题通关、无错通关等行为授予徽章或称号。
- 公开排行榜：展示用户积分、等级、连续打卡天数等基础数据。

游戏化视觉应克制：以终端、命令块、进度节点和简约图标为主，不采用复杂的重度游戏画面。

#### 4.3.1 成就体系

成就定义为**纯数据**（`backend/app/achievements/catalog.py`），判定逻辑独立（同目录 `__init__.py`），
与课程内容「内容与逻辑分开」的做法一致。共 **22 条**，分 4 组：

| 分组 | 条数 | 覆盖的行为 |
| --- | --- | --- |
| 坚持 | 3 | 连续打卡 3 / 7 / 21 天 |
| 通关 | 11 | 完成 1 / 5 / 10 / 21 个单元，通关 6 个区域各自与全部 |
| 无错 | 3 | 1 / 3 / 6 个单元「首次作答即正确」 |
| 成长 | 5 | 累计 500 / 2000 / 5000 经验，达到等级 5 / 10 |

**判定时机**：只在两个写路径上 —— 提交作答（`POST /api/v1/quests/{id}/submit`）与打卡
（`POST /api/v1/checkin`）。每次全量重算该用户的统计量并解锁所有已满足项，**幂等且只增不减**：
连续打卡断掉后连续天数归零，但已经拿到的徽章不收回。

**「无错通关」的口径**：一个单元内全部题目都已完成，**且**没有任何一题答错过。
为此新增 `quest_attempts` 表记录每次作答的对错；**只在题目尚未完成时写入** ——
完成之后的重复提交属于练习，不应追溯破坏此前的无错记录。

新解锁的成就会随作答 / 打卡响应一起返回（`achievements` 字段），前端据此弹出提示。

#### 4.3.2 实现状态

| 项 | 状态 |
| --- | --- |
| 任务闯关（按顺序解锁单元与区域） | ✅ `compute_unit_statuses()` |
| 技能树成长 | ✅ `build_skill_tree()` + `SkillTreePanel.vue` |
| 每日打卡与连续天数 | ✅ `do_checkin()` + `ProgressCard.vue` |
| 成就与称号 | ✅ 22 条成就 + `GET /api/v1/achievements` + `AchievementsView.vue` |
| 公开排行榜 | ✅ `GET /api/v1/leaderboard` + `LeaderboardView.vue` |
| 解锁即时提示 | ✅ `AchievementToast.vue`（右下角，6 秒自动消失） |

**排行榜实现口径**：按经验值降序、连续打卡天数降序排列（并列时按用户 id 升序保证稳定）。
**只展示用户名、等级、经验值、连续打卡天数**，不展示邮箱与答题详情（见 4.5）。
接口公开可读；登录用户额外返回 `me`，即使排在 `limit` 之外也能看到自己的名次。

**排行榜走 SQL 聚合，未接入 Redis**（2026-09 决策）。需求 §7 把 Redis 列为「缓存/排行榜/会话」的
技术选型，但 MVP 规模下 `ORDER BY xp DESC, streak_days DESC LIMIT N` 毫秒级返回；
引入 Redis 会多出第二份真相与一个硬依赖（本地开发必须起 Redis 才能跑）。
**确有性能需求时再接入，届时以 MySQL 为准、Redis 作只读缓存。**

### 4.4 命令查询

命令查询是 ShellQuest 的第二条主线：训练之外，用户需要一个**随时可查、内容可信、且能回到练习**的命令手册。

#### 4.4.1 对标与定位

首期以 [Linux 命令搜索引擎](https://wangchujiang.com/linux-command/index.html) 为对标基准（开源仓库：[jaywcjlove/linux-command](https://github.com/jaywcjlove/linux-command.git)），做到**内容规模与详情页结构对齐**，并在其基础上补充 ShellQuest 特有能力：

| 能力 | 参考站点 | ShellQuest |
| --- | --- | --- |
| 命令数量 | 600+ | 首期对齐，全量导入 |
| 索引方式 | 仅按字母 A–Z | 字母索引 **+ 主题分类 + 标签** |
| 详情页结构 | 简介 / 补充说明 / 语法 / 选项 / 参数 / 实例 / 扩展知识 | 对齐，并增加「关联任务」「相关命令」 |
| 检索 | 命令名与简介搜索 | **关键词搜索 + 自然语言搜索**，结果可跳转任务 |
| 与课程联动 | 无 | **双向跳转** |

核心差异：参考站点是独立手册；ShellQuest 的命令查询**必须与课程、闯关打通**，否则只是重复造一个已有产品。

#### 4.4.2 内容来源与许可

- 内容来源：`jaywcjlove/linux-command`，**MIT 许可**，允许商用与二次分发。
- 获取方式（`scripts/import_commands.py` 的实现）：
  - 索引：`dist/data.json`
  - 详情：`command/<命令名>.md`
  - **批量获取走 npm tarball**（`registry.npmjs.org/linux-command/-/linux-command-<版本>.tgz`），
    一次请求拿到全部 614 篇 Markdown 与 `LICENSE`。逐文件请求 unpkg 需要 600+ 次往返且易被限流，
    内容完全一致，因此默认用 tarball；`--source-dir` 可指向任意已解包的目录（含 unpkg 下载结果）。
- 导入在**构建 / 维护期离线完成**，内容落库到本项目数据库；运行时**不依赖**外部站点，避免可用性与网络风险。
- 导入结果同时导出为 `backend/data/commands_seed.json.gz` 并**随仓库提交**。
  容器构建期没有网络、运行期数据库又是空的，启动时若 `commands` 表为空则从该快照自动灌入。
- 必须在命令详情页与「关于」页展示 MIT 许可与来源声明（详情接口已返回 `license` 与 `source_url`）。
- 导入脚本必须**幂等**：按内容哈希比对，重复执行结果一致，并输出「新增 / 更新 / 未变 / 删除」四项计数。
- **更新策略：手动触发，不自动同步。** 上游为社区仓库、持续变动，自动同步会引入不可预期的内容变更与解析失败。建议节奏为季度一次，或在大版本发布前执行。

#### 4.4.3 内容结构化的现实约束

上游 600+ 篇 Markdown 由 50+ 贡献者长期维护，**章节标题的写法并不统一**。对 614 篇全文的实测结果：

| 结构要素 | 实测情况 |
| --- | --- |
| 一级标题 | **614/614 全部为 setext 形式**（第一行是命令名，第二行是 `===`），无 ATX 形式 |
| 一句话简介 | **613/614 在 H1 之后紧跟恰好 1 个段落** |
| 章节标题 | 层级混乱：`## 补充说明` 出现 529 次但 `### 语法` 出现 493 次、`## 语法` 仅 18 次 |
| 标题别名 | 示例段有 `实例`(363) / `例子`(15+62) / `示例`(6) 三种写法；语法段有 `语法` / `概要` 两种 |
| 代码围栏 | 2 篇含零宽字符污染（`netstat.md` 的 `​```shell`），1 篇围栏不配平（`curl.md`） |
| 选项格式 | 至少 4 种：`-a  # 说明`、`-a：说明`、`-h, --help  说明`、`-f 说明` |

因此首期采用**「原文忠实渲染 + 尽力提取」**的混合模型，不强行全量结构化：

- **必须准确**：命令名、一句话简介、原文全文（`body_markdown`）。
- **分区尽力归一**：按标题文本（而非层级）映射到规范角色 ——
  说明 / 语法 / 选项 / 参数 / 示例 / 返回值 / 注意 / 扩展知识；**未识别的标题一律保留**为 `extra`，
  按原顺序参与渲染，不丢弃内容。
- **结构化字段尽力提取**：`syntax` / `options` / `examples`。提取失败（或围栏不配平）时置空，
  前端降级为渲染对应分区的原文段落。

实测提取成功率（基于上游 v1.22.0）：

| 字段 | 有该章节的命令 | 提取成功 | 覆盖率 |
| --- | --- | --- | --- |
| `options` | 499 | 461 | 92% |
| `examples` | 466 | 456 | 98% |
| `sections` | 614 | 613 | 99.8%（仅 `curl.md` 因围栏不配平放弃结构化） |

#### 4.4.4 命令数据模型

| 字段 | 说明 | 来源 | 首期必需 |
| --- | --- | --- | --- |
| `name` | 命令名，唯一，用于 URL | 文件名 | 是 |
| `summary` | 一句话中文简介 | 上游索引 / 正文首段 | 是 |
| `body_markdown` | 原始 Markdown 全文 | `command/<name>.md` | 是 |
| `source_url` | 上游文件地址 | 生成 | 是 |
| `license` | 许可标识（固定 `MIT`） | 常量 | 是 |
| `search_text` | 全文检索用拼接字段 | 生成 | 是 |
| `category` | 主题分类，取值域见 4.4.6 | ShellQuest 自建映射 | 是 |
| `tags` | 标签数组，含上游分类 | ShellQuest 自建 + 上游分类 | 否 |
| `syntax` | 语法 | 解析语法段 | 否 |
| `options` | 选项列表（选项 + 说明） | 解析选项段 | 否 |
| `examples` | 示例列表（说明 + 命令 + 可选输出） | 解析示例段 | 否 |

**分类策略**

**上游不提供任何分类数据。** `dist/data.json` 只有 `n`（名称）/ `p`（路径）/ `d`（简介）三个字段，
站点列表页也是纯 A–Z 平铺，没有「文件管理 / 磁盘管理」这类功能分组。

因此 `category` 与 `tags` **全部自建**，维护在 `backend/scripts/command_taxonomy.json`，不写死在代码里：

- `category` 取值域 = 课程主题地图的 6 大区域 + `其他`，与课程使用同一套语言。
- `tags` 为功能标签（文件 / 目录 / 文本 / 权限 / 压缩归档 / 进程 / 用户 / 磁盘 / 服务 / 日志 /
  网络 / 端口 / 传输 / 安全 / 包管理 / Shell内建 / 排障 / 性能 …），用于精确检索与相关命令推荐。
- 无法归入任一课程区域的命令（如 `mail`、`dpkg-split`）统一归入 `其他`，不强行塞进 6 个区域。

映射表用「正则规则（按序匹配）+ 显式覆盖」两层结构：规则覆盖命令族（如 `^docker`、`^(iptables|ip6tables|…)`），
显式条目用于纠正个别命令。启动与导入时都会校验分类值是否在合法取值域内，写错直接报错。

#### 4.4.5 命令详情页结构

按以下顺序呈现，与参考站点对齐：

1. 命令名 + 一句话简介
2. 主题分类与标签
3. 补充说明
4. 语法
5. 选项
6. 参数
7. 示例（多组，每组含说明文字与终端提示符）
8. 扩展知识
9. **关联任务**（ShellQuest 增强）
10. **相关命令**（ShellQuest 增强）
11. 来源与许可

示例区需保留上游的终端提示符风格（`$` 与 `[root@localhost ~]#`），并在深色终端主题下保证可读。

#### 4.4.6 浏览与索引

- **字母索引**：按命令名首字符分组（`#`、`A`–`Z`），支持点击跳转。
- **主题分类**：按 `category` 分组浏览，取值域为课程主题地图的 6 大区域 + `其他`（见 4.4.4）。
- **标签筛选**：支持多标签组合筛选。
- 列表项统一为「命令名 + 一句话描述」，与参考站点一致。
- 600+ 条数据需支持分页或虚拟滚动，首屏不得阻塞。

#### 4.4.7 搜索

两种入口共用结果展示。

**关键词搜索**

- 匹配范围与权重（高 → 低）：命令名 > 标签 > 一句话简介 > 选项 > 正文。
- 命令名精确命中时置顶。
- 结果展示：命令名、命中上下文片段（高亮）、关联任务入口。

**自然语言搜索**

- 用户输入「查看某个端口被谁占用」这类描述，返回匹配命令与示例。
- 首期采用**轻量关键词 / 规则匹配**（见第 10 节已确认项），不接入大模型。
- **映射词表来源**：不单独维护词表文件，而是从**课程关卡声明**中派生。每个关卡在内容数据里声明它涉及的 `commands` 与 `phrasings`（问题描述），「命令 ↔ 任务」的关联与「描述 → 命令」的映射由同一份声明生成，避免两处各写一套而漂移。
- **覆盖范围**：首期覆盖 21 个课程单元涉及的全部命令（约 40–60 条）；其余命令由关键词搜索兜底。

**交互**

- `/` 聚焦搜索框，`Esc` 清空，`↑` `↓` 切换候选，`Enter` 打开。
- 输入即时联想，命令名前缀优先。
- 无结果时给出相近命令建议，而非空白页。

#### 4.4.8 与课程的双向联动

| 方向 | 入口 | 行为 |
| --- | --- | --- |
| 命令 → 任务 | 命令详情页「关联任务」 | 展示使用该命令的课程单元与关卡，可直接进入 |
| 任务 → 命令 | 任务页命令标签 | 跳转到对应命令详情 |

关联关系需**显式维护**，不依赖文本猜测。数据来源见 4.4.7：由关卡内容声明中的 `commands` 字段生成，与自然语言映射**同源**。首期至少覆盖 21 个课程单元中出现的全部命令。

#### 4.4.9 首期范围

- **全量导入**上游 600+ 命令的原文与简介，不做子集精选。导入是自动化的，600 条与 60 条的工程量相同；而手册一旦出现「查不到」，用户就会流失。
- 完成字母索引、主题分类、标签筛选三种浏览方式。
- 完成关键词搜索与轻量自然语言搜索。
- 完成命令与课程的双向跳转。
- 结构化字段（语法 / 选项 / 示例）**尽力提取**，不阻塞上线。

#### 4.4.10 首期不做

- 不接入真实 man page 渲染或 `--help` 抓取。
- 不做用户投稿、编辑、评论。
- 不做高级中文分词检索；首期使用数据库模糊匹配 + 关键词映射。
- 不做命令的版本差异对比（GNU / BSD / busybox）。

#### 4.4.11 验收标准

以下各项均由 `backend/scripts/verify_commands.py` 自动核查（在已导入的真实库上只读运行）：

- [x] 可浏览全部导入命令，字母索引与主题分类均可正常跳转。（614 条 / 27 个首字母 / 7 个分类 / 47 个标签）
- [x] 随机抽取 20 条命令，详情页内容与上游一致，无内容丢失。（**614/614 条 `body_markdown` 与种子快照逐字节一致**）
- [x] 输入命令名可准确命中并置顶。（抽样 20 条，20/20 置顶）
- [x] 输入「查看端口占用」「磁盘满了怎么查」等自然语言描述，能返回正确命令。（见 4.4.12 实测表）
- [x] 从命令详情页可跳转到关联任务，从任务页可跳回命令详情。（浏览器实测 `/commands/ls` → `/units/2`，答题后 `pwd` → `/commands/pwd`）
- [x] 命令详情页展示 MIT 许可与来源声明。
- [x] 600+ 条数据下列表页首屏可交互，滚动不卡顿。（每页 40 条，共 16 页）

**已知例外**：`curl` 一条命令的 Markdown 围栏不成对，章节切分失败，详情页回落渲染上游原文（内容不丢，只是没有结构化章节）。详见 `AGENTS.md` §7。

#### 4.4.12 实现状态

| 项 | 状态 |
| --- | --- |
| 上游导入脚本（幂等 + 报告） | ✅ `backend/scripts/import_commands.py` |
| 分类 / 标签映射表 | ✅ `backend/scripts/command_taxonomy.json` |
| 离线种子快照 | ✅ `backend/data/commands_seed.json.gz`（614 条，925 KB） |
| 命令数据表（`commands` / `command_tags`） | ✅ 已实现 |
| 列表 / 分面 / 详情接口 | ✅ `GET /api/v1/commands`、`/facets`、`/{name}` |
| 关键词搜索（字段加权） | ✅ 见 4.4.7 权重表；命中课程声明的命令额外加权 |
| 自然语言搜索（从关卡声明派生） | ✅ `GET /api/v1/commands/natural` |
| 无结果时的相近命令建议 | ✅ 响应 `suggestions` 字段（`suggest_commands()`，词元化 + IDF 加权） |
| 端到端冒烟测试 | ✅ `backend/scripts/smoke_test.py`（13 项断言，临时库） |
| 验收标准自动核查 | ✅ `backend/scripts/verify_commands.py`（真实库只读，覆盖 4.4.11 全部条目） |
| 命令 ↔ 任务双向跳转的数据 | ✅ 详情接口返回 `related_quests`，任务接口返回 `commands` |
| 前端浏览 / 搜索 / 详情页 | ✅ `CommandsView.vue`（A–Z 索引 / 分类 / 标签筛选 / 分页 / 关键词 / 自然语言 / 键盘操作）、`CommandDetailView.vue`（章节 / 选项表 / 实例 / 原文折叠 / 关联卡片） |
| 命令 ↔ 任务的双向跳转入口 | ✅ 答题正确后展示「涉及命令」→ 命令详情；详情页「课程关联」卡片 → 单元 |
| 筛选状态可分享 | ✅ 全部筛选条件写进 URL query（`?q=&mode=&category=&tag=&letter=&page=`），刷新 / 分享 / 后退均保留 |

**自然语言检索的实测效果**（12 组问法，11 组命中正确单元为第 1 名）：

| 输入 | 命中的关卡 |
| --- | --- |
| 查看某个端口被谁占用 | U11 端口冲突 |
| 磁盘空间不足怎么办 | U9 磁盘告警 |
| 服务重启后起不来 | U8 服务没有启动 |
| 部署脚本没有执行权限 | U5 权限事故 |
| 怎么把文件传到远程服务器 | U12 远程发布 |
| 统计日志里出现最多的 IP | U13 日志筛查管道 |
| 新同事只能看日志目录怎么授权 | U6 用户与访问 |

**已知局限**：课程只覆盖 21 个主题，像「怎么压缩一个目录」这类问题在课程里没有对应关卡，
自然语言匹配会给出勉强的结果。此时以关键词检索结果作为兜底建议返回（`suggestions` 字段），
前端应同时展示「自然语言结果」与「关键词结果」。

兜底建议不能把整句直接丢给关键词检索 —— `keyword_search()` 用 `LIKE` 粗筛，
「查看某个端口被谁占用」不是任何命令的子串，必然零命中。实现上先做词元化
（中文只取 2-gram，3-gram 过于具体），逐个词元检索后按 `得分 × IDF` 跨词元累计；
同时丢弃命中面超过 25% 语料的泛词（否则「目录」会盖过「压缩」）。实测：

| 输入 | 兜底建议（前 4） |
| --- | --- |
| 怎么压缩一个目录 | `gzip` `ls` `bunzip2` `bzip2` |
| 查看某个端口被谁占用 | `lsof` `docker` `ss` `iftop` |
| 怎么看磁盘还剩多少空间 | `df` `du` `quota` `quotacheck` |
| 怎么批量改文件名 | `rename` `chpasswd` `pssh` `rm` |

### 4.5 账号与进度

- 基础账号注册、登录和退出；首期仅支持用户名 + 密码登录。
- 面向公众开放注册，不采用邀请制。
- 保存课程进度、关卡状态、答题记录、经验值、技能树和打卡数据。
- 训练路径（§3）作为账号设置持久化在服务端，换设备登录后仍是上次选择的那一种。
- 公开排行榜按经验值降序、连续打卡天数降序排列；同一任务重复完成不重复获得经验。
- 排行榜仅展示用户名、等级、经验值和连续打卡天数；不展示邮箱、答题详情等私密数据。

## 5. 核心用户流程

1. 用户注册或登录后，选择“21 天训练营”或“自由闯关”。
2. 用户进入课程/主题地图，选择当前可挑战的任务。
3. 用户阅读任务场景，在模拟终端或题目中完成命令练习。
4. 系统立即反馈结果，并提供正确用法和相关知识。
5. 用户获得经验与技能点；满足条件时解锁后续关卡、成就或技能节点。
6. 完成当天目标后，系统记录打卡与连续学习天数。
7. 用户可随时通过速查或自然语言搜索解决临时命令问题。

## 6. 交互与视觉原则

- 简洁、紧凑、信息优先，风格贴近开发者常用的终端与 IDE。
- 默认使用深色终端风格；浅色模式作为后续可选功能。
- PC 端采用课程/地图、任务主区、进度侧栏的高效布局；移动端优先展示当前任务与底部操作区。
- 模拟终端应支持命令输入、历史记录展示、清晰的成功/失败状态，但不伪装为真实系统环境。
- 任务反馈采用“结果 → 原因 → 正确做法 → 延伸学习”的顺序。
- 排行榜与成就作为辅助入口，不干扰主学习流程。

## 7. 技术方案与部署约束

| 层级 | 技术 |
| --- | --- |
| 前端 | Vue 3 + TypeScript |
| 后端 | Python + FastAPI |
| 数据库 | MySQL |
| 缓存/排行榜/会话等 | Redis |
| 部署 | Docker 容器化部署 |

- 应提供 Docker Compose 方式的一键本地或服务器部署。
- 前后端、MySQL、Redis 应以独立服务组织，并通过环境变量配置连接信息。
- 首期以 Docker Compose 的开发机/服务器部署为目标，并预留生产环境变量配置；域名、HTTPS 与云服务选型后续确定。
- 界面需兼容 PC 与移动端主流浏览器。

## 8. 验收标准（MVP）

- [x] 用户可完成注册、登录，并在不同设备上查看自己的学习进度。（进度全部落库，按账号隔离）
- [x] 用户可选择 21 天训练营或自由闯关，并可进入对应的课程/主题任务。（`users.training_mode` +
  `POST /api/v1/user/mode`；`camp` 严格串行、`free` 不设关卡锁，切换不丢进度，见 §3「实现状态」）
- [x] 至少覆盖首期定义的 Linux 学习主题，并具备 21 天完整课程结构。（6 区域 / 21 单元 / 63 题）
- [x] 每个任务支持至少一种模拟练习形式，并可立即给出反馈与解析。（4 种题型 + 5 种判题器）
- [x] 完成任务能更新经验、技能树和打卡状态，且能按规则解锁内容。
- [x] 用户可使用命令速查及自然语言搜索找到相关命令与示例（细则见 4.4.11）。
- [x] 用户可从命令详情页跳转到关联任务，并从任务页跳回命令详情。
- [x] 命令内容标注 MIT 许可与来源。
- [x] 用户可查看公开排行榜。（`GET /api/v1/leaderboard`，公开可读）
- [ ] 项目可通过 Docker Compose 启动全部所需服务。**未验证**：`docker-compose.yml` 已就绪，
  但本轮开发全程使用本地 SQLite + Vite，未实际执行过 `docker compose up`（开发机未安装 Docker）
- [x] 核心页面在 PC 和移动端均可正常使用。（390px 视口已逐页验证）

> 标 `[x]` 的条目均有可复现的验证依据：`backend/scripts/smoke_test.py`（37 项断言，含训练路径 9 项）、
> `backend/scripts/verify_commands.py`（4.4.11 全部条目）、以及浏览器端的逐页走查
> （命令速查 13 项、成就/排行榜、训练路径 camp ⇄ free 往返）。

## 9. 后续扩展（不纳入 MVP）

- 好友系统、组队学习、团队/企业排行榜。
- 真实隔离式 Linux 容器终端与自动命令判题。
- 更多课程路径、个性化推荐与错题复习。
- 社区题目、用户自建关卡与内容审核。
- 更丰富的成就、赛季和活动机制。

## 10. 待决问题

- [x] 21 天课程的每日主题、任务数量与难度递进：**已确定**。21 个单元归属 6 大区域，
  每单元 3 道题（2 道场景任务 + 1 道快速巩固），共 63 道题；内容见 `backend/app/curriculum/`，
  设计意图见 `COURSE_DESIGN.md`（见 4.1.2）。
- [x] 命令查询首期导入规模：**全量导入** 600+ 条，不做子集精选（见 4.4.9）。
- [x] 命令主题分类：**自建**，取值域为课程 6 大区域 + `其他`；上游分类降级为标签（见 4.4.4）。
- [x] 上游内容更新：**手动触发、不自动同步**，导入脚本幂等，建议季度一次或大版本前执行（见 4.4.2）。
- [x] 「问题描述 → 命令」映射词表：**从课程关卡声明派生**，与「命令 ↔ 任务」关联同源；首期覆盖 21 个单元涉及的命令（见 4.4.7）。
- [x] 命令查询对标 Linux 命令搜索引擎（`jaywcjlove/linux-command`，MIT 许可），首期导入其命令原文与简介。
- [x] 项目名称为 ShellQuest；默认采用深色终端风格。
- [x] 自然语言搜索首期采用轻量的关键词/规则匹配；模型能力留待后续评估。
- [x] 账号首期仅支持用户名 + 密码、公开注册。
- [x] 排行榜按经验值和连续打卡天数排序，并仅展示基础公开学习数据。
- [x] 首期支持 Docker Compose 的开发机/服务器部署；域名与 HTTPS 后续确定。
- [ ] 命令查询模块的检索排序权重（精确匹配 / 前缀匹配 / 模糊匹配的优先级）。
- [ ] 成就与徽章的具体条目清单（4.3）。
